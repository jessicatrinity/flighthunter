"use client";
import {useState} from "react";
export default function Home(){const [status,setStatus]=useState("");return <main><section className="hero"><div className="eyebrow">FlightHunter / V0.1</div><h1>Find the flight nobody else found.</h1><p>Search conventional fares, alternate airports, and creative route combinations from one place.</p></section><section className="panel"><div className="grid"><div className="field"><label>FROM</label><input defaultValue="YEG" placeholder="Airport or city"/></div><div className="field"><label>TO</label><input placeholder="Airport, city, region, or Anywhere"/></div><div className="field"><label>TRAVEL MONTH</label><input type="month"/></div><div className="field"><label>MAX PRICE (CAD)</label><input type="number" placeholder="500"/></div><div className="field"><label>MAX STOPS</label><select defaultValue="1"><option value="0">Direct only</option><option value="1">1 stop</option><option value="2">2 stops</option></select></div><div className="field"><label>BAGGAGE</label><select defaultValue="carryon"><option value="carryon">Carry-on required</option><option value="checked">Checked bag required</option><option value="none">No requirement</option></select></div></div><div className="actions"><button className="primary" onClick={()=>setStatus("Search engine scaffold ready. Next: connect live flight inventory.")}>Find flights</button><button className="secondary" onClick={()=>setStatus("Auto-buy is intentionally disabled in V0.1.")}>Auto-buy rules</button></div>{status&&<div className="status">{status}</div>}</section></main>}